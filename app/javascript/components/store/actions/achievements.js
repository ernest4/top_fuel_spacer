import { createAction } from "redux-actions";

export const setCurrentAchievementId = createAction("SET_CURRENT_ACHIEVEMENT_ID");
export const setAchievementById = createAction("SET_ACHIEVEMENT_BY_ID");
