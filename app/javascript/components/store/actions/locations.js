import { createAction } from "redux-actions";

export const setCurrentLocationId = createAction("SET_CURRENT_LOCATION_ID");
export const generateNextLocations = createAction("GENERATE_NEXT_LOCATIONS");
