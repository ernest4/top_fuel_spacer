import { createAction } from "redux-actions";

export const setCurrentConversationId = createAction("SET_CURRENT_CONVERSATION_ID");
export const setConversationsShow = createAction("SET_CONVERSATIONS_SHOW");
