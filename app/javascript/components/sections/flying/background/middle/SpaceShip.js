import React from "react";
import Spacing from "../../../../layout/Spacing";

const SpaceShip = () => {
  return <Spacing {...{ background: "pink", children: "space ship wip" }} />;
};

export default SpaceShip;
