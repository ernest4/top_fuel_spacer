import React from "react";
import PageButton from "./PageButton";

const Achievements = () => {
  return <PageButton secondary {...{ pageId: 6, children: "Achievements" }} />;
};

export default Achievements;
